export enum EProjectType {
  MOBILE = "mobile",
  WEB = "web",
  BACKEND = "backend",
}

export enum EDemoMode {
  IFRAME = "iframe",
  EXTERNAL = "external",
  EMULATOR = "emulator",
}

export enum EEmulatorPlatform {
  IOS = "ios",
  ANDROID = "android",
}

export enum EEmulatorOrientation {
  PORTRAIT = "portrait",
  LANDSCAPE = "landscape",
}

export enum EErrorCode {
  VALIDATION_ERROR = "VALIDATION_ERROR",
  UNAUTHORIZED = "UNAUTHORIZED",
  NOT_FOUND = "NOT_FOUND",
  CONFLICT = "CONFLICT",
  INVALID_FILE = "INVALID_FILE",
  RATE_LIMITED = "RATE_LIMITED",
  INTERNAL_ERROR = "INTERNAL_ERROR",
}

export enum EThemeMode {
  DARK = "dark",
  LIGHT = "light",
}
